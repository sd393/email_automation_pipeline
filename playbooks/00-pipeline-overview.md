# Purpose

One-paragraph summary of the 5-stage pipeline (source domains → discover contacts → verify emails → compose emails → send), plus Stage 0 (brief interview) and Stage 6 (bounce polling). Fills in the mental model for Claude before any stage runs. Filled out further in section 12.

# When Claude reads this

At campaign start, after Stage 0 completes and before invoking Stage 1, to remind itself of the overall shape and where the human-in-the-loop checkpoints are (Phase A → Phase B approval gate).
